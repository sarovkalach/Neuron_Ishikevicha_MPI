    free(V);
    V = NULL;

    free(u);
    u = NULL;

    free(V_node);
    V_node = NULL;

    free(u_node);
    u_node = NULL;

    free(I_syn);
    I_syn = NULL;

    free(I_syn_node);
    I_syn_node = NULL;

    free(part_I_syn_node);
    part_I_syn_node = NULL;

    free(I_ext);
    I_ext = NULL;

    free(I_ext_node);
    I_ext_node = NULL;

    free(V_prev);
    V_prev = NULL;

    free(u_prev);
    u_prev = NULL;

    free(V_prev_node);
    V_prev_node = NULL;

    free(u_prev_node);
    u_prev_node = NULL;

    free(k_V_node);
    k_V_node = NULL;

    free(k_u_node);
    k_u_node = NULL;

    free(V_tmp_node);
    V_tmp_node = NULL;

    free(u_tmp_node);
    u_tmp_node = NULL;

    free(pre_con_node);
    pre_con_node = NULL;

    free(post_con_node);
    post_con_node = NULL;

    free(weights_con_node);
    weights_con_node = NULL;

    free(y_con_prev_node);
    y_con_prev_node = NULL;

    free(y_con_curr_node);
    y_con_curr_node = NULL;

    free(neur_spike_node);
    neur_spike_node = NULL;

    free(t_spike_node);
    t_spike_node = NULL;
    
    free(neur_spike_znode);
    neur_spike_node = NULL;

    free(t_spike_znode);
    t_spike_node = NULL;
